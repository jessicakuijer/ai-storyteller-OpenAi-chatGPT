<div id="response" class="bg-blue-100 rounded-lg py-5 px-6 mb-4 text-base text-blue-700 mb-3">
    {% if json %}
        <blockquote>
        {# <img src="{{asset('images/storytelling.jpeg')}}" /> #}
            "{{ json }}"
            <footer>
            
                <cite>-- 🤖 Auteur(e) doué(e) d'intelligence artificielle.</cite>
            </footer>
        </blockquote>
    {% endif %}
</div>