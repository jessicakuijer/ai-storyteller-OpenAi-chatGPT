{% extends 'base.html.twig' %}

{% block body %}

    <h1 class="text-center text-darkred-600 text-5xl font-bold mb-6">🧞‍♂️ Raconte-moi mon HISTOIRE! 👩‍🎤 🧌</h1>

    {{form_start(form,
        {
            attr: {
                'class': 'block p-8 rounded-lg shadow-lg bg-white max-w-2xl container mx-auto max-h-fit',
            }
        })}}
    {{form_row(form.story, {
        choice_label: 'alternativeStory',
        attr: {
            'class': 'lighter form-control block w-full px-3 py-1.5 text-base font-normal text-gray-700 bg-white bg-clip-padding border border-solid border-gray-300 rounded transition ease-in-out m-0 focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none flex flex-row'
        }
    }) }}
    {{form_row(form.submit, {
        label_html: true,
        label: 'C\'est parti!!! <img style="width:28px;height:28px;" class="htmx-indicator" src="/loader.svg"/>',
        attr: {
            'class': 'w-full px-6 py-2.5 bg-blue-600 text-white font-medium text-xs leading-tight uppercase rounded shadow-md hover:bg-blue-700 hover:shadow-lg focus:bg-blue-700 focus:shadow-lg focus:outline-none focus:ring-0 active:bg-blue-800 active:shadow-lg transition duration-150 ease-in-out flex flex-row place-content-center'
        }
    }) }}
    {{form_end(form)}}

    <div class="block p-8 rounded-lg shadow-lg bg-white max-w-2xl container mx-auto max-h-fit mb-5 mt-2">Si tu es sur PC ou MAC, utilise la reconnaissance vocale!<button id="recButton" class="buttonrec"></button></div>
    <div id="response"></div>

{% endblock %}
